# Public values available from Supabase > Project settings > API.
# Keep this file free of real credentials.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Keep disabled until the Azure provider is configured in Supabase and Microsoft Entra ID.
NEXT_PUBLIC_ENABLE_MICROSOFT_AUTH=false

